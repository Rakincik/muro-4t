# MURO — Provisioning Scripts

## Kullanım

### Yeni Kurum Ekleme (add-tenant.ps1)
```powershell
.\scripts\add-tenant.ps1 -Name "ABC KPSS Kursu" -Code "abckpss" -Subdomain "abckpss" -ServerGroup "srv-2"
```

### Tüm DB'lere Migration Uygulama (migrate-all.ps1)
```powershell
.\scripts\migrate-all.ps1
```
